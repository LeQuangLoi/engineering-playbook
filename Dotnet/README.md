# .NET

Notes for .NET development, testing, packaging, and operations.

## Scope

Add SDK, project, testing, or runtime notes here when there is a concrete .NET workflow to preserve. Create a subfolder only when it contains a real page or source artifact.

Use a separate Markdown page for each repeatable workflow and record the SDK version when a procedure depends on it.
